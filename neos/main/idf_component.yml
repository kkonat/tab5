dependencies:
  espressif/elf_loader: "*"
  espressif/m5stack_tab5_noglib: "*"
  # The BSP pulls espressif/usb (for the UVC camera). Every 1.x release we can
  # resolve declares idf>=5.4 but calls usb_dwc_hal functions that do not exist
  # until a later IDF, so it will not compile on 5.4.2. Nothing here uses USB,
  # so point the dependency at IDF 5.4.2s own usb component instead.
  espressif/usb:
    version: "*"
    override_path: "C:/ESP-IDF/esp-idf/v5.4.2/esp-idf/components/usb"
